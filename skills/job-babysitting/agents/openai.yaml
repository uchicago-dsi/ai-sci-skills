interface:
  display_name: "Job Babysitting"
  short_description: "Monitor jobs, detect stalls, triage failures"
  default_prompt: "Use $job-babysitting to monitor this job directly until its terminal condition, detect stalls, and take the next justified action."
